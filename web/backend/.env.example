# Copy to .env and fill in values

# Modal auth token (or run `modal token new` to authenticate interactively)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# Frontend origin for CORS (production)
FRONTEND_ORIGIN=https://your-vercel-app.vercel.app
